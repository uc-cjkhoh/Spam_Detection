[Current Pipeline]
1. Setup required components (database, model, etc)
2. Check if has existing model
    -> if no
        -> Stratified sampling by time of day
        -> Save then in local as .xlsx (Excel)
        -> Terminate module
        -> Developer manually label the first batch of data
        -> Re-run the module 
        -> Move to step 1
    -> if yes:
        -> Initialize or Train Model
        -> Move to step 3
3. Select another batch of data with stratified sampling method
4. Normalize data
5. Convert data to vectors
6. Perform dimension reduction
7. Classify them with the model
8. Label high confidence sms as `1`, confidenec score >= <threshold>
9. Label manually <n> uncertain sms, where their confidence score should be closest to |p - 0.5| as `-1`
10. Label the rest as `0` 
11. Save or update metadata of the result and data 
12. Repeat step 3 - 11

 
[Problems] 
1. *** What is the most suitable way to evaluate the model while lack of labeled data ? 
We split data into train and validation data, 


2. * What if I found out the previous model do better job and want to re-train starting from there ? 
3. 


[To-do List]
1. Create a loop for partial fit 
