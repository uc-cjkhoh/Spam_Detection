server:
  source: 'mysql'
  host: "10.168.51.196"
  port: 3306
  user: 'unified'
  password: 'unified'

data:
  query: 
    'SELECT 
      `smsc_src_addr`, 
      `payload_length`,
      `payload` 
    FROM 
      sms_spam_cd.data_tdr_spam_filter 
    LIMIT 500'
  column_name: ['from', 'message_length',  'message'] # need to match the number of columns from `query`
  target_column: 'message'
  drop_null: True
  drop_duplicates: False

models:
  text_embedding:
    model_name: 'jinaai/jina-embeddings-v3'
    batch_size: 4